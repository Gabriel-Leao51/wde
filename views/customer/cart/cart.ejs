<%- include('../../shared/includes/head', { pageTitle: t('cart.yourCartTitle') }) %>
<link rel="stylesheet" href="/styles/cart.css">
<script src="/scripts/cart-item-management.js" defer></script>
</head>
<body>
  <%- include('../../shared/includes/header') %>
  <main>
    <h1><%= t('cart.yourCartHeading') %></h1>
    <ul id="cart-items">
      <% for (const cartItem of locals.cart.items) { %>
        <li>
          <%- include('includes/cart-item', { item: cartItem }) %>
        </li>
      <% } %>
    </ul>
    <div id="cart-total">
      <p><%= t('cart.total') %>: $<span id="cart-total-price"><%= locals.cart.totalPrice.toFixed(2) %></span></p>

      <% if (locals.isAuth && locals.cart.totalQuantity > 0) { %>
        <form action="/orders" method="POST">
          <input type="hidden" name="_csrf" value="<%= locals.csrfToken %>">
          <button class="btn"><%= t('cart.buyProducts') %></button>
        </form>
      <% } else { %>
        <p id="cart-total-fallback"><%= t('cart.loginToProceed') %></p>
      <% } %>
    </div>
  </main>
<%- include('../../shared/includes/footer') %>