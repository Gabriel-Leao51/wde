<%- include('../../shared/includes/head', { pageTitle: t('auth.otpVerifyTitle') }) %>
  <link rel="stylesheet" href="/styles/forms.css">
  <link rel="stylesheet" href="/styles/auth.css">
</head>
<body>
  <%- include('../../shared/includes/header') %>
  <main>
    <h1><%= t('auth.otpVerifyHeading') %></h1>
    <p><%= t('auth.otpVerifyInstructions') %></p>
    <% if (inputData.errorMessage) { %>
      <section class="alert">
        <h2><%= t('auth.invalidInputHeading') %></h2>
        <p><%= inputData.errorMessage %></p>
      </section>
    <% } %>
    <form action="/login/otp/verify" method="POST">
      <input type="hidden" name="_csrf" value="<%= locals.csrfToken %>">
      <input type="hidden" name="email" value="<%= inputData.email %>">
      <p>
        <label for="code"><%= t('auth.otpCodeLabel') %></label>
        <input
          type="text"
          id="code"
          name="code"
          inputmode="numeric"
          pattern="[0-9]{6}"
          maxlength="6"
          autocomplete="one-time-code"
          value="<%= inputData.code %>"
          required>
      </p>
      <button class="btn"><%= t('auth.otpVerifyButton') %></button>
      <p id="switch-form"><a href="/login/otp"><%= t('auth.otpRequestNewCode') %></a></p>
    </form>
  </main>
<%- include('../../shared/includes/footer') %>
