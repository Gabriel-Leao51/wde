<%- include('../../shared/includes/head', { pageTitle: t('auth.otpRequestTitle') }) %>
  <link rel="stylesheet" href="/styles/forms.css">
  <link rel="stylesheet" href="/styles/auth.css">
</head>
<body>
  <%- include('../../shared/includes/header') %>
  <main>
    <h1><%= t('auth.otpRequestHeading') %></h1>
    <p><%= t('auth.otpRequestInstructions') %></p>
    <% if (inputData.errorMessage) { %>
      <section class="alert">
        <h2><%= t('auth.invalidInputHeading') %></h2>
        <p><%= inputData.errorMessage %></p>
      </section>
    <% } %>
    <form action="/login/otp/request" method="POST">
      <input type="hidden" name="_csrf" value="<%= locals.csrfToken %>">
      <p>
        <label for="email"><%= t('auth.emailLabel') %></label>
        <input type="email" id="email" name="email" value="<%= inputData.email %>" required>
      </p>
      <button class="btn"><%= t('auth.otpSendCodeButton') %></button>
      <p id="switch-form"><a href="/login"><%= t('auth.otpBackToPasswordLogin') %></a></p>
    </form>
  </main>
<%- include('../../shared/includes/footer') %>
